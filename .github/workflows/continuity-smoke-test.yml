name: Continuity smoke test

on:
  push:
    branches:
      - 'docs/eventix-style-continuity'
    paths:
      - '.github/workflows/continuity-smoke-test.yml'

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "continuity workflow ok"
